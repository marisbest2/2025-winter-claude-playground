{
  "name": "teaneck-meeting-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "prettier": "^3.7.4",
    "typescript": "^5"
  }
}
