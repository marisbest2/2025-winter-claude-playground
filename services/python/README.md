# Python Services

## Setup

```bash
cd services/python
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

## Services

Add Python services here when needed (e.g., PDF processing, ML tasks)
